Run global_planner.py

Select the start point on the map by left-clicking on the required position, and select the goal position by right-clicking on the required position.
They will be shown in red and blue respectively
You can change the positions by left or right clicking again on the map, which replaces the previously placed start/goal point respectively.

When the correct points are selected, press enter to start the algorithm.
